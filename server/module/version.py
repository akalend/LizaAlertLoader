import time

from module.cache import Cache

    # print(request.matchdict['id'])
    # print('mode=' + conf['mode'])
def echo(request):

    mode = conf['mode']
    # print(mysql_conf[mode])
    return Response('TunnelGlobe API V 1.0 <br>https://tunnelglobe.com')
